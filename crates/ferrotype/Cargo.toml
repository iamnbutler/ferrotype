[package]
name = "ferrotype"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Rust-to-TypeScript type mapping for Zed RPC"

[dependencies]
ferrotype-derive = { path = "../ferrotype-derive" }
linkme = "0.3"

[dev-dependencies]
insta = "1.41"
trybuild = "1.0"
