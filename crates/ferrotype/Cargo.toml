[package]
name = "ferrotype"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Rust-to-TypeScript type mapping for Zed RPC"

[dependencies]

[dev-dependencies]
insta = "1.41"
